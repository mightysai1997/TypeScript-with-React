import React from 'react';

const HardcodedCredentials: React.FC = () => {
  const username = 'admin';
  const password = 'password123'; // Hardcoded credentials

  return <div>Credentials are hardcoded!</div>;
};

export default HardcodedCredentials;
